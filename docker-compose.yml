version: "3"

services:
  demo:
    image: hoopoed/RESR-Management:latest
    volumes:
      - /home/daishengle20050809/app.db:/usr/app/data/app.db
    ports:
      - "80:8000"
    env_file:
      - .env